# Backlog de mejoras

Sale de [AUDITORIA.md](AUDITORIA.md). Un ítem por ciclo, en orden de prioridad,
y cada uno se cierra con su verificación completa antes del commit.

Estados posibles, `pendiente`, `en curso`, `hecho`, `bloqueado`.

| id | prioridad | ítem | estado | notas |
|---|---|---|---|---|
| M01 | 1 | Migrar los tests a pytest, con `conftest.py`, fixtures compartidas de catálogo de ejemplo, una sola lista de tests, y cobertura en el CI con umbral que no baje | hecho | Cubre T1, T2, T4 y T6. 267 tests donde antes `pytest -q` colectaba cero. La lista quedó sólo en `testpaths` de `pyproject.toml`; `build/build.py` y el CI la consumen de ahí, y el build aborta si pytest no colecta nada. Umbral de cobertura en 55 %, medido en 56,18 %. De yapa, pyright pasó de 97 errores a 39. **Quedó afuera**, el test de la CSP (es M02) y `ruff format`, que falla en 25 archivos de todo el repo desde antes y es M03. Las capturas se regeneran bien pero se revirtieron: el re-render es ruido ajeno a este ítem. |
| M02 | 2 | Test de la cabecera `Content-Security-Policy` | hecho | T3. Doce tests nuevos. Cubren que la cabecera esté, cada directiva por separado, que ninguna abra un origen externo, que `'unsafe-inline'` siga sólo en `style-src` y no en `default-src` ni en `script-src`, y que la página no incumpla su propia política (ningún `<script>` inline, ningún recurso por CDN, ninguna tipografía de afuera). Se verificó que fallan de verdad aflojando la CSP a mano. El README decía «dos defensas, las dos con test propio»; ahora son tres y dice tres. No se tocó `app/server.py`. |
| M03 | 3 | `ruff` (check y format) y `pyright` en estricto progresivo, con `pre-commit` configurado. Arreglar lo que salte | hecho | D4. pyright pasó de 39 errores a 0, en modo `basic` y con el camino a `strict` escrito en `pyproject.toml`. Corre en su propio job del CI, con las dependencias opcionales instaladas, porque el job de tests corre sin ellas a propósito. `ruff format` se adoptó en un commit aparte, mecánico y revertible; el costo es que colapsa los comentarios alineados a la derecha, y está dicho en el README. `pre-commit` corre ruff y `build/sin_claves.py`, que ahora es la única implementación del control de claves y la comparte con el CI. **Salió de acá un arreglo real**, `relevar_core.py` usaba `urllib.error` sin importarlo nunca: funcionaba de rebote porque `urllib.request` lo importa por dentro. |
| M04 | 4 | Tipar los contratos entre módulos con dataclasses o TypedDict, lo que devuelve `relevar_core` y consumen `productos`, `validar`, `portadas` y `paquete` | hecho | C1. `contratos.py` con once TypedDict. **TypedDict y no dataclasses**, porque los datos se arman por partes y se serializan a JSON tal cual; dataclasses obligaba a reescribir unos doscientos accesos `p["title"]` a cambio de nada que el usuario note. El set de cadenas escrito a mano de `test_migrar_core` salió, ahora sale de `Relevamiento.__annotations__`, y se comprobó que falla sacándole una clave a `relevar()`. **El verificador encontró tres campos que viajaban entre módulos sin estar documentados en ningún lado**, `tidal`, `tidal_cobertura` y `audio_error`, y un guard flojo en el cambio de canal Topic. |
| M05 | 5 | `CONTRIBUTING.md`, plantillas de issue, y corregir los errores de redacción del README | hecho | D3 y P1. `CONTRIBUTING` en los dos idiomas, con las seis reglas que no se negocian escritas por primera vez en algún lado. Dos plantillas de issue en formato de formulario, más `config.yml` que apaga el issue en blanco y manda los reportes de seguridad al formulario privado. Los dos errores de redacción, corregidos: la imagen sin línea en blanco de `README.md:350` y el docstring de `build/capturas.py` que decía «claro» donde el código genera en oscuro. Se verificaron además todos los enlaces relativos de los quince documentos (ninguno roto), que las dos versiones del README tengan la misma estructura (22 encabezados, en el mismo orden) y las afirmaciones contables del README (los trece glifos son trece). |
| M06 | 6 | Unificar los normalizadores de texto duplicados y desacoplar `paquete.py` de `audio.py` | hecho | E3 y E4. `texto.py` con una implementación de cada cosa, y 28 tests nuevos, entre ellos uno que verifica que los tres módulos apunten a la misma función para que no se vuelvan a bifurcar. `FORMATOS_LOSSLESS` pasó a `contratos.py` y `paquete` ya no importa `audio`, lo que verifica un test leyendo su AST. **Salieron tres cosas de acá**, `audio._norm` no sacaba los acentos y eso hacía que un artista con tilde no encontrara en Tidal al mismo sin ella; el slug de carpeta podía dejar un punto al final después de recortar, que en Windows es un nombre inválido; y `validar._sin_acentos` estaba definida y no se usaba en ningún lado. |
| M07 | 7 | Tests de contrato contra respuestas reales grabadas de Deezer e iTunes, en fixtures JSON, sin red | hecho | T5. Siete fixtures grabadas con `build/grabar_fixtures.py`, que es lo único del repositorio que toca la red, cada una con su URL y su fecha adentro. Quince tests que corren el cliente real con la red reemplazada. La cobertura subió de 56,27 % a 59,10 %. **Lo que salió a la luz**, la búsqueda estricta de Deezer devuelve cero resultados para un tema que sí está en su catálogo, así que el respaldo por búsqueda libre no es un caso de borde sino el camino habitual, y ahora hay un test que lo demuestra con la respuesta real. Se graba además el camino del error (una búsqueda de iTunes sin resultados), que es el que nadie graba. |
| M08 | 8 | Generar las notas del release desde el `CHANGELOG`, en vez del bloque escrito a mano en el workflow | hecho | D2, y con eso queda cerrado el ítem original de versionado, cuyo resto ya estaba. `build/notas_release.py` arma las notas en los dos idiomas y **aborta si el CHANGELOG no tiene entrada para la versión que se taguea**, que era la forma de publicar un release que miente. El bloque `body:` del workflow pasó de 125 líneas a una. La prosa que no es un cambio (cuál bajar, cómo verificar la firma) vive en `build/notas/`. Los enlaces relativos del CHANGELOG se pasan a absolutos, porque adentro de un release GitHub los resuelve contra la URL del release y dan 404. `build/build.py` dejó de tener su propia lectura de `VERSION` y usa la misma función. 20 tests nuevos. |
| M09 | 9 | Paquete `src/migrador/` con los módulos que hoy están en la raíz, lanzadores a `scripts/`, y `pyproject.toml` con extras `[app]`, `[audio]`, `[dev]`. Preservar `python app/launcher.py` y `python build/build.py` | hecho | E1 y E2. Los diez módulos del motor pasaron a `src/migrador/` con imports relativos, y el `sys.path.insert` de `relevar_core` desapareció. `app/` consume el paquete y la flecha va en un solo sentido. `python app/launcher.py` y `build/build.py` andan **sin instalar nada**, y además el paquete se instala con `pip install -e .[app,audio,dev]`, que es lo que hace el job de tipos del CI. La versión se mudó a `src/migrador/version.py`, único lugar donde está escrita. **Dos trampas que aparecieron**, el workpath de PyInstaller se llamaba `build/migrador` y habría sido tomado como paquete de espacio de nombres (renombrado a `_pyinstaller`), y el `source` de cobertura enumeraba módulos por nombre, así que al moverlos dejó de medirlos y la cobertura «subió» sola; ahora es por carpeta. Umbral subido de 55 a 58. |
| M10 | 10 | Empaquetado `.app` para macOS y AppImage o `.deb` para Linux, aunque sigan sin firmar, y actualizar el README | hecho | D1. macOS, `BUNDLE` en el spec más un `.icns` de once tamaños escrito a mano (Pillow sólo lo guarda en macOS, y el icono se genera en el CI). Linux, **`.deb` y no AppImage**, porque se arma con la biblioteca estándar y no obliga a bajar `appimagetool` ni a tener FUSE en el runner; además se puede verificar desde cualquier sistema, y 29 tests lo hacen. El paquete es reproducible byte por byte, así que el SHA256 publicado sirve para comparar. `--instalador` ahora significa lo mismo en los tres sistemas. **Lo que NO se pudo verificar acá**, que el `.app` abra en una Mac y que el `.deb` instale en un Ubuntu: esta máquina es Windows. Lo que sí se verificó es la estructura de los dos y que el spec y el builder corran. La promesa de «está pendiente» salió del README. |
| M11 | 11 | Detalles, los trece `# noqa: BLE001` inertes, `log=print` como default en once funciones, y la diferencia entre Python 3.14 local y 3.13 del CI | hecho | C2, C4 y D5. `BLE` prendido en ruff, y eso destapó **22 capturas amplias más que no tenían ninguna nota**: ahora las 34 llevan escrito al lado por qué están, y una nueva falla el CI. Una se acotó de verdad, el parseo del error de YouTube, que no toca red ni librerías y ahora nombra sus cuatro excepciones, con siete casos de prueba. El piso de Python decía 3.9 en `build.py` mientras el proyecto declaraba 3.13, así que compilar con 3.9 pasaba el control y fallaba después; ahora se lee de `requires-python`. El CI corre los tests en 3.13 y en 3.14. **`log=print` quedó como está, a propósito**, ver abajo. Lo de `capturas.py` se resolvió en M05. |
| M12 | cerrado | Resolver `ux-audit/`, moverlo a `docs/` o borrarlo | hecho | D6. No requiere ningún cambio en el repositorio. Ya está en `.gitignore` con su justificación escrita, y `git ls-files ux-audit` no devuelve nada, así que nunca fue parte del repo público. Lo que queda es una decisión de escritorio de quien mantiene, borrar la carpeta local o dejarla; no es trabajo de código. |

## Qué quedó afuera del backlog, y por qué

**C4, `log=print` como valor por defecto.** Once funciones públicas escriben a
stdout si nadie les pasa un callback, y eso es una impureza real en una
librería. Se evaluó en M11 y se decidió dejarlo.

El motivo es que el arreglo cuesta más de lo que rinde. Cambiar el default a una
función vacía haría que cualquier script que hoy llama a `migrar()` y ve el
avance en la consola deje de verlo, sin error y sin aviso, que es la peor forma
de romper algo. Y el supuesto problema no lastima a nadie: adentro del
ejecutable, compilado sin consola, esos `print` van al vacío, y la app siempre
pasa su propio callback. Es una impureza que se nota leyendo y no usando.

**C3, el estado global del servidor.** `TOKEN`, `AUDIO_HABILITADO`, `ESTADO` y
`JOBS` se calculan al importar `app/server.py`. Es incómodo para testear, pero
está argumentado en el módulo y es coherente con una app local de un solo
usuario. Convertirlo en inyección de dependencias sería un refactor grande, sin
beneficio para quien usa la app, y con riesgo directo sobre las tres defensas.
Si algún ítem futuro lo necesita de verdad, entra ahí y no como tarea propia.

**P3 y P4**, la heurística de formato por cantidad de tracks y el módulo de
audio apagado por defecto. Se revisaron y están bien declarados en el código y en
el README. No son deuda, son decisiones.
